apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-pv-storage
spec:
  capacity:
    storage: 3Gi
  accessModes:
    - ReadWriteMany
  storageClassName: local-path
  hostPath:
    path: /ssd/nfs
    type: DirectoryOrCreate